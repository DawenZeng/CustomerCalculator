<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="image_margin">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="Calculator_symbol">
        <item name="android:background">@drawable/keyboard_equal</item>
        <item name="android:enabled">true</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/input_screen_key</item>
    </style>

    <style name="Calculator_number">
        <item name="android:background">@drawable/keyboard</item>
        <item name="android:enabled">true</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/input_screen_key</item>
    </style>

</resources>
